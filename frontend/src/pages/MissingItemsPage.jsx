import React,{useEffect,useState} from 'react'; import {Link} from 'react-router-dom'; import Navbar from '../components/Navbar'; import Footer from '../components/Footer'; import api from '../services/api';
export default function MissingItemsPage(){const [items,setItems]=useState([]),[search,setSearch]=useState(''); const load=async()=>setItems((await api.get('/missing',{params:{search}})).data||[]); useEffect(()=>{load()},[]); return <div className="min-h-screen flex flex-col bg-gray-50"><Navbar/><main className="flex-1 max-w-7xl w-full mx-auto px-4 py-9"><div className="flex flex-wrap justify-between gap-3 mb-6"><div><h1 className="text-3xl font-extrabold">Students Looking for These</h1><p className="text-sm text-gray-500">Recently reported missing items across Anurag University.</p></div><Link to="/create-missing" className="bg-blue-600 text-white px-4 py-3 rounded-xl font-bold text-sm">+ Post Missing Item</Link></div><form onSubmit={e=>{e.preventDefault();load()}} className="flex gap-2 mb-6"><input className="flex-1 border rounded-xl px-4 py-3" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search name, brand, color, or location"/><button className="bg-gray-900 text-white rounded-xl px-5 font-bold">Search</button></form>{items.length===0?<div className="bg-white rounded-3xl p-12 text-center text-gray-500">No active missing-item reports right now.</div>:<div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-5">{items.map(i=><div key={i._id} className="bg-white border rounded-2xl overflow-hidden shadow-sm">{i.imageUrl&&<img src={i.imageUrl} className="w-full h-40 object-cover"/>}<div className="p-4"><span className="text-[10px] bg-rose-100 text-rose-700 rounded-full px-2 py-1 font-black">MISSING</span><h2 className="font-extrabold text-lg mt-2">{i.itemName}</h2><p className="text-xs text-gray-500">{i.brand} · {i.color}</p><p className="text-xs mt-2">Last seen: <b>{i.lastKnownLocation}</b></p><Link to={`/missing/${i._id}`} className="mt-4 block text-center bg-rose-600 text-white rounded-xl py-2.5 font-bold text-xs">I FOUND THIS</Link></div></div>)}</div>}</main><Footer/></div>}
